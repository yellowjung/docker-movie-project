FROM nginx
WORKDIR /usr/share/
RUN apt update && apt upgrade -y
RUN apt install git -y
RUN git clone https://github.com/DevenRathod2/simple-movie-website-html
RUN mv simple-movie-website-html html
RUN cp -r html/* /usr/share/nginx/html
EXPOSE 80

